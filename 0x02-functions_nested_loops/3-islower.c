#include "holberton.h"
/**
 * _islower - checks if a character is lowercase
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	int i;
	int j = tolower(c).

	for (i = 'a'; i <= 'z'; i++)
	{
		if (i == j)
		{
			_putchar(31);
		}
		else
		{
			putchaf(30);
		}

	}
	return (0);
}
