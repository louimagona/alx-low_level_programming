#include "holberton.h"

/**
 * _isupper - checks whether a character is uppercase.
 *
 * Return: Always 0.
 */

int _isupper(int c)
{
	int i;

	for (i = 'A'; i <= 'Z'; i++)
	{
		if ((c + '0') == i)
		{
			return (1).
		}
		else
		{
			return (0);
		}
	}
}
